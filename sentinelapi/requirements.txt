# SentinelAPI engine requirements.
# Use a standard CPython 3.11-3.13 (python.org or Anaconda), NOT a MSYS2/MinGW
# build - MinGW Pythons have no PyPI binary wheels and force source builds.
# Lower-bound pins so pip picks a prebuilt wheel that matches your interpreter.

# === Core ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9

# === Database (SQLite) ===
sqlmodel>=0.0.22

# === OpenAPI parsing ===
prance>=23.6.21.0
openapi-spec-validator>=0.7.1
pyyaml>=6.0.1

# === AI / LLM (optional at runtime - scanner works without a key) ===
langchain>=0.3.0
langchain-groq>=0.2.0

# === Security (optional but recommended: encrypts tokens at rest) ===
# If this fails to build on your platform, the app still runs with a labelled
# non-encrypted fallback. Comment it out only if you cannot install it.
cryptography>=42.0.0

# === CLI ===
typer>=0.12.0
rich>=13.7.0

# === SSE ===
sse-starlette>=2.1.0

# === Utilities ===
python-dotenv>=1.0.0

# === Testing ===
pytest>=8.3.0
pytest-asyncio>=0.24.0
