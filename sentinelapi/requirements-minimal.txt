# Escape hatch: everything except cryptography (which needs a compiler/Rust on
# exotic Python builds). The credential vault falls back to a labelled
# non-encrypted store. Use only if `pip install -r requirements.txt` fails on
# cryptography and you cannot switch to a standard CPython.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9
sqlmodel>=0.0.22
prance>=23.6.21.0
openapi-spec-validator>=0.7.1
pyyaml>=6.0.1
langchain>=0.3.0
langchain-groq>=0.2.0
typer>=0.12.0
rich>=13.7.0
sse-starlette>=2.1.0
python-dotenv>=1.0.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
