FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi==0.115.0 uvicorn[standard]==0.30.0 pydantic==2.9.0

COPY sentinelshop /app/sentinelshop

EXPOSE 4000

CMD ["uvicorn", "sentinelshop.app:app", "--host", "0.0.0.0", "--port", "4000"]
